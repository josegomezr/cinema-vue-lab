(function(window, Vue, $){
  var MovieRow = Vue.component('movie-row', {
    template: '#movie-row-tpl',
    props:{
      funcion: {
        type: Object,
      }
    },
    computed: {

    },
  })
})(window, Vue, jQuery);
